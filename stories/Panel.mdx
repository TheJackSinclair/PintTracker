import { Meta } from "@storybook/blocks";
import PanelStories from './Panel.stories';

<Meta title="Panel" of={PanelStories} />

# Panel
`Panel` provides a styled container for information or other components.

### Usage
Use `Panel` as a host for other content, such as `AnalyticsChart` or `BeerGlass`.