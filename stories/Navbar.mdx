import { Meta } from "@storybook/blocks";
import NavbarStories from './Navbar.stories';

<Meta title="Navbar" of={NavbarStories} />

# Navbar
`Navbar` is a navigation bar, providing users with access to different pages of the application.

### Usage
`Navbar` appears on all pages as a central navigation element to navigate between pages.