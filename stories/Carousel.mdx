import { Meta } from "@storybook/blocks";
import CarouselStories from './Carousel.stories';

<Meta title="Carousel" of={CarouselStories} />

# Carousel
`Carousel` dynamically displays content, allowing users to click through and view multiple items.

### Usage
Use `Carousel` as an interative component to display several user metrics and analytics on the MyPints dashboard. 