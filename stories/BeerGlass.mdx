import { Meta } from "@storybook/blocks";
import BeerGlassStories from './BeerGlass.stories';

<Meta title="BeerGlass" of={BeerGlassStories} />

# BeerGlass
`BeerGlass` is an decorative component that simulates the look of a beer glass. It is an animated, interactive component which tilts on-hover. 
