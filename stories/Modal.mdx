import { Meta } from "@storybook/blocks";
import ModalStories from './Modal.stories';

<Meta title="Modal" of={ModalStories} />

# Modal
`Modal` is an overlay that appears over the main content to focus the user's attention on a specific task or message.

### Usage
Use the `Modal` to capture user input or display important information that requires an immediate decision or acknowledgment from the user.