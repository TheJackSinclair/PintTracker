import { Meta, Props } from "@storybook/blocks";
import AnalyticsCardStories from './AnalyticsCard.stories';

<Meta title="AnalyticsCard" of={AnalyticsCardStories} />

# AnalyticsCard
`AnalyticsCard` is used to display key data points or pint statistics in a card format.

### Usage
Use `AnalyticsCard` to highlight important pint metrics or analytics on the MyPints dashboard or homepage.
