import { Meta } from "@storybook/blocks";
import ButtonStories from './Button.stories';

<Meta title="Button" of={ButtonStories} />

# Button
`Button` is an interactive component, allowing users to trigger actions or submit forms with a single click.

### Usage
Use `Button` in Dialogs, Modals, and across pages to submit forms, trigger actions, or receive any interactive feedback.
