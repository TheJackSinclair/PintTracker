import { Meta } from "@storybook/blocks";
import AnalyticsChartStories from './AnalyticsChart.stories';

<Meta title="AnalyticsChart" of={AnalyticsChartStories} />

# AnalyticsChart
`AnalyticsChart` visually represents data changes over time, such as weekly pint consumption statistics.

### Usage
Use `AnalyticsChart` for displaying trends in their pint data on the MyPints dashboard or homepage.